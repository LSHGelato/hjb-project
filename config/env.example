# Historical Journals & Books (HJB) Project
# Environment Variables & Secrets Management
#
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit .env file to Git (add to .gitignore)
# 2. Each developer/deployment should have their own .env file
# 3. Use strong, unique passwords for all accounts
# 4. Store backups of .env securely (encrypted external drive, vault service)
# 5. Rotate credentials periodically
# 6. On shared systems (like OrionMX), restrict file permissions: chmod 600 .env

################################################################################
# MYSQL DATABASE CREDENTIALS
################################################################################

# HostGator MySQL database
HJB_MYSQL_HOST="raneywor.hosting.mysql.database.com"
HJB_MYSQL_PORT="3306"
HJB_MYSQL_DATABASE="raneywor_hjbproject"

# Application user (limited privileges)
# Permissions: SELECT, INSERT, UPDATE, DELETE, EXECUTE
HJB_MYSQL_USER="raneywor_app_hjb"
HJB_MYSQL_PASSWORD="your_strong_app_password_here"

# Admin user (for schema migrations only)
# Permissions: ALL PRIVILEGES (or specific DDL permissions)
# Only use this in migrations, NEVER in application code
HJB_MYSQL_ADMIN_USER="raneywor_admin_hjb"
HJB_MYSQL_ADMIN_PASSWORD="your_strong_admin_password_here"

################################################################################
# MEDIAWIKI BOT ACCOUNT
################################################################################

# MediaWiki bot user (for API access to publish pages)
# Create this user on the MediaWiki with bot rights
HJB_WIKI_USER="HJBBot"
HJB_WIKI_PASSWORD="your_mediawiki_bot_password_here"

# Optional: API token (some wikis support token-based auth)
HJB_WIKI_API_TOKEN=""

################################################################################
# INTERNET ARCHIVE CREDENTIALS
################################################################################

# Internet Archive (optional; most downloads don't require auth)
# Get API key from: https://archive.org/account/s3.php
IA_API_KEY=""
IA_SECRET_KEY=""

################################################################################
# HATHITRUST CREDENTIALS
################################################################################

# HathiTrust API credentials (if using authenticated downloads)
# Most HathiTrust content is open; auth usually not needed
HATHI_API_KEY=""

################################################################################
# DEPLOYMENT & ENVIRONMENT
################################################################################

# Current environment
ENVIRONMENT="production"  # development, staging, production

# Node identifier (set per machine)
NODE_NAME="orionmx"  # orionmx, orionmega, etc.

# Logging level for this deployment
LOG_LEVEL="INFO"  # DEBUG, INFO, WARNING, ERROR

################################################################################
# OPTIONAL: EMAIL ALERTS
################################################################################

# Send alert emails on task failures (future feature)
# SMTP_SERVER="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="alerts@example.com"
# SMTP_PASSWORD="your_email_password_here"
# ALERT_RECIPIENT_EMAIL="admin@example.com"

################################################################################
# OPTIONAL: MONITORING & ANALYTICS
################################################################################

# Sentry (error tracking, if integrated in future)
# SENTRY_DSN=""

# DataDog or other monitoring (if integrated in future)
# DATADOG_API_KEY=""

################################################################################
# PROXY SETTINGS (if behind corporate firewall)
################################################################################

# HTTP/HTTPS proxy for external API calls
# HTTP_PROXY=""
# HTTPS_PROXY=""
# NO_PROXY="localhost,127.0.0.1,.local"

################################################################################
# DEVELOPMENT OVERRIDES
################################################################################

# Only set these if doing local development (NOT production)
# DATABASE_OFFLINE_MODE="false"  # Set true to test without DB
# USE_MOCK_IA_DOWNLOADS="false"  # Set true to use mock data instead of real IA
